#open("ชื่อไฟล์","โหมด","รหัส") ไม่ระบุก้ได้
#Mode	Description
"""
โหมดหลัก
r : อ่านอย่างเดียว (ไฟล์ ต้องมีอยู่แล้ว)
w : เขียนอย่างเดียว (ถ้ามีไฟล์เดิม 👉 ลบทับทั้งไฟล์; ถ้าไม่มี 👉 สร้างใหม่)
a : เขียนต่อท้ายอย่างเดียว (ไม่มีไฟล์ 👉 สร้างใหม่; ตัวชี้เขียนอยู่ท้ายไฟล์เสมอ)
ผสมอ่าน-เขียน (+)
r+ : อ่าน/เขียน (ไฟล์ต้องมีอยู่แล้ว, ไม่ลบทับทั้งไฟล์)
w+ : อ่าน/เขียน (ถ้ามี 👉 ลบทับทั้งไฟล์, ถ้าไม่มี 👉 สร้างใหม่)
a+ : อ่าน/เขียนแบบต่อท้าย (ไม่มี 👉 สร้างใหม่; การเขียนจะต่อท้ายเสมอ แม้ seek(0))
ไบนารี (b)
เติม b เมื่อทำงานกับข้อมูลไบนารี เช่น รูป/เสียง/โมเดล: rb, wb, ab, rb+, wb+, ab+
ถ้าไม่ใส่ b คือ text mode (อ่าน/เขียนเป็นสตริง; ใช้ encoding= ได้)
ตัวเสริมที่มักใช้
x : สร้างไฟล์ใหม่เท่านั้น (ถ้ามีอยู่แล้ว 👉 เกิด FileExistsError)
t : text mode (ค่าเริ่มต้นอยู่แล้ว จึงแทบไม่ต้องใส่)
"""

#"รหัส" คือ ระบุก้ได้ไม่ระบุก้ได้ ภาษาอื่นๆที่ต้องเอนโค้ดเพื่ออ่าน 

# a=open("C:\\Users\\WTFTHE\\Desktop\\code\\Python\\1Python_&_Preliminary\\EP92.2.txt","r")
a=open('1_Python_&_Preliminary/EP92.2.txt','r',encoding="utf-8")
b=open('1_Python_&_Preliminary/EP92.3.txt','r',encoding="utf-8")#"utf-8"สำหรับภาษาไทย ถ้ามีภาษาไทยต้องใส่
c=open('1_Python_&_Preliminary/EP92.3.txt','r',encoding="utf-8")

print(a)
print(a.read())
print(a.read())#read ได้ครั้งเดียว ควรทำแบบนี้ x = a.read()
print('..')

print(b)
print(b.read())
print('...')

x = c.read(50)#5ตัวตัวแรก
print("x",x[:15])
print("x2",x)
print('....')

with open("1_Python_&_Preliminary/EP92.3.txt", "r", encoding="utf-8") as f:
    text = f.read()
    print(text)


'''
    .read()# "line1\nline2\nline3" / .readlines()# ["line1\n", "line2\n", "line3"]
    .read().splitlines()  / lines = [line.rstrip("\n") for line in f.readlines()] # ไม่มี \n แล้ว
'''
